services:
  web:
    # build: .
    image: ghcr.io/akatgelar/fastapi-postgres-testcase:latest
    container_name: 'fastapi-postgres-testcase'
    env_file:
      - .env
    ports:
      - "2040:8000"
    # volumes: 
    restart: always
